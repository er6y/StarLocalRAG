[package]
name = "tokenizers-ffi"
version = "0.1.0"
edition = "2021"

[lib]
name = "tokenizers_ffi"
crate-type = ["cdylib"]

[dependencies]
tokenizers = { path = "../tokenizers-rust/tokenizers" }
serde_json = "1.0"
jni = "0.19.0"
